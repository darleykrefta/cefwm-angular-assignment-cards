import { SuitColorizedPipe } from './suit-colorized.pipe';

describe('SuitColorizedPipe', () => {
  it('create an instance', () => {
    const pipe = new SuitColorizedPipe();
    expect(pipe).toBeTruthy();
  });
});
